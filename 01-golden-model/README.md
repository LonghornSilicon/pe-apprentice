# Step 01: Golden Model

**Week 5, first half.** Not written yet.

A Python model that computes what the PE should output, from the specification,
so your scoreboard has something to compare against.

`gen_pe_vectors.py` has the Q8.8 helpers in it and the model itself left for
you. The handout for this step is still being written. Ask a lead.
